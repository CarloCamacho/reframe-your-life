---
phase: 01-home-screen-static-tree-ui
plan: 01
type: execute
wave: 1
depends_on: [01-03]
files_modified:
  - src/data/mockTrees.js
  - src/components/Home/HomeScreen.jsx
  - src/components/Home/TreeCard.jsx
autonomous: true
requirements: [HOME-01, HOME-02, HOME-03]

must_haves:
  truths:
    - "Home screen renders a list of mock trees showing name, date, and status"
    - "One tree card shows a linked-tree indicator (spawned via Go Deeper)"
    - "'Start New Tree' CTA button is visible and calls setActiveTree on click"
    - "Layout is mobile-first with calm, muted styling and Kellian Labs branding"
  artifacts:
    - path: "src/data/mockTrees.js"
      provides: "Mock tree array with 3 trees including one linked-tree example"
      exports: ["MOCK_TREES"]
    - path: "src/components/Home/HomeScreen.jsx"
      provides: "Tree list screen with branding and CTA"
      exports: ["default"]
    - path: "src/components/Home/TreeCard.jsx"
      provides: "Individual tree summary card with linked-tree indicator"
      exports: ["default"]
  key_links:
    - from: "src/components/Home/HomeScreen.jsx"
      to: "src/store/appStore.js"
      via: "useAppStore — setActiveTree to navigate to tree view"
      pattern: "useAppStore.*setActiveTree"
    - from: "src/components/Home/HomeScreen.jsx"
      to: "src/data/mockTrees.js"
      via: "import MOCK_TREES"
      pattern: "import.*MOCK_TREES"
---

<objective>
Build the home screen showing a list of mock trees with a "Start New Tree" CTA and Kellian Labs branding.

Purpose: Delivers HOME-01, HOME-02, HOME-03 — the entry point users see on every visit, listing their trees with status, dates, and linked-tree indicators.
Output: HomeScreen.jsx, TreeCard.jsx, mockTrees.js
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@docs/AGENTS.md

<interfaces>
<!-- This plan depends on the Zustand store and Tailwind theme tokens created by Plan 03 (Wave 0). -->
<!-- The executor MUST wait for Plan 03 to complete first (depends_on: [01-03]). -->

From src/store/appStore.js (created by Plan 03):
```js
export const useAppStore = create((set) => ({
  currentView: 'home',        // 'home' | 'tree'
  activeTreeId: null,          // string | null
  setView: (view) => set({ currentView: view }),
  setActiveTree: (id) => set({ activeTreeId: id, currentView: 'tree' }),
  goHome: () => set({ currentView: 'home', activeTreeId: null }),
}))
```

From src/index.css (theme tokens added by Plan 03):
```css
@theme {
  --color-surface: #111827;
  --color-surface-raised: #1f2937;
  --color-text-primary: #f9fafb;
  --color-text-muted: #6b7280;
}
```
Use these as Tailwind classes: `bg-surface`, `bg-surface-raised`, `text-text-primary`, `text-text-muted`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mock tree data</name>
  <files>src/data/mockTrees.js</files>
  <action>
Create `src/data/mockTrees.js` exporting a `MOCK_TREES` array with 3 tree objects:

1. "Find more meaningful work" — status: 'active', no linked tree
2. "Start my own business" — status: 'active', linkedFromTreeId: 'tree-1', linkedFromNodeId: 'r3a' (this is the linked-tree example for HOME-03)
3. "Improve my relationship with money" — status: 'complete', no linked tree

Each object shape:
```js
{
  id: string,
  name: string,
  createdAt: Date,
  updatedAt: Date,
  status: 'active' | 'paused' | 'complete',
  linkedFromTreeId: string | null,
  linkedFromNodeId: string | null,
}
```

Use realistic dates (March-April 2026). This matches the Firestore data model from AGENTS.md section 5.
  </action>
  <verify>
    <automated>node -e "import('./src/data/mockTrees.js').then(m => { if(m.MOCK_TREES.length !== 3) throw 'wrong count'; if(!m.MOCK_TREES.find(t => t.linkedFromTreeId)) throw 'no linked tree'; console.log('OK') })"</automated>
  </verify>
  <done>MOCK_TREES exports 3 trees, one with linkedFromTreeId set</done>
</task>

<task type="auto">
  <name>Task 2: Build HomeScreen and TreeCard components</name>
  <files>src/components/Home/HomeScreen.jsx, src/components/Home/TreeCard.jsx</files>
  <action>
**TreeCard.jsx** — Receives a single tree object as prop. Renders:
- Tree name (text-text-primary, font-medium)
- Status badge: 'active' = green dot, 'paused' = yellow dot, 'complete' = grey checkmark
- "Created" and "Last updated" dates formatted as relative or short date (e.g. "Mar 15" or "Apr 1")
- If `linkedFromTreeId` is not null, show a linked-tree indicator: a small chain-link icon or text like "Linked from another tree" in text-text-muted — this satisfies HOME-03
- Card has `bg-surface-raised rounded-xl p-4 cursor-pointer` styling
- onClick calls `useAppStore.getState().setActiveTree(tree.id)` to navigate to tree view

**HomeScreen.jsx** — The full home screen:
- Top section: "Kellian Labs" branding text (text-text-muted, text-xs, uppercase tracking-widest) and app name "Reframe Your Life" (text-2xl font-bold text-text-primary)
- "Start New Tree" button: prominent CTA, rounded-full, uses the rung-2-active blue colour as background (`bg-rung-2-active text-white`). onClick calls `setActiveTree('new')` (the tree view will handle the 'new' case in a later phase)
- Tree list: maps over MOCK_TREES, renders a TreeCard for each, ordered by updatedAt descending
- Layout: `max-w-md mx-auto px-4 py-8` for mobile-first centering with generous whitespace
- If no trees exist (empty array), show a gentle message: "No trees yet. Start your first reframe."
- Import MOCK_TREES from `src/data/mockTrees.js`
- Import useAppStore from `src/store/appStore.js`

Design principles from AGENTS.md section 10: calm UI, muted palette, generous whitespace, mobile-first.
  </action>
  <verify>
    <automated>cd /Users/ianf/Library/CloudStorage/OneDrive-Personal/Coding/reframe-your-life && bun run build 2>&1 | tail -5</automated>
  </verify>
  <done>HomeScreen renders 3 TreeCards with names, dates, status badges; one card shows linked-tree indicator; "Start New Tree" button visible; Kellian Labs branding present; build succeeds</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries in this plan. All data is hard-coded mock data. No user input, no network requests, no auth.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | N/A | All | accept | Phase 1 is static mock data only — no user input, no API calls, no persistence |
</threat_model>

<verification>
- `bun run build` succeeds with no errors
- Dev server (`bun run dev`) shows home screen at localhost:5173
- Three tree cards visible with names, dates, and status indicators
- "Start my own business" card shows linked-tree indicator
- "Start New Tree" button is visible and styled prominently
- Kellian Labs branding text appears at top
- Layout centres on mobile viewport (max-w-md)
</verification>

<success_criteria>
HOME-01: Tree list with name, date, status — visible on screen
HOME-02: "Start New Tree" CTA — visible, clickable
HOME-03: Linked tree indicator — visible on "Start my own business" card
</success_criteria>

<output>
After completion, create `.planning/phases/01-home-screen-static-tree-ui/01-01-SUMMARY.md`
</output>
