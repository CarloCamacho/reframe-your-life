import { describe, it } from 'vitest'

describe('TreeCard', () => {
  it.todo('shows linked-tree indicator when linkedFromTreeId is set (HOME-03)')
  it.todo('renders tree name, date, and status')
})
